Contains the following macros:

If TEST_ON is defined, replaces "Test_Mode(something)" with "something".

Replaces "PRINT(X)" "std::cout<<(#X)<<"="<<(X)<<std::endl;"

Furthermore includes "cassert".



